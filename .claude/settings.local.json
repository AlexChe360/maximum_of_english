{
  "permissions": {
    "allow": [
      "Bash(mix:*)",
      "Bash(HEX_HTTP_CONCURRENCY=1 HEX_HTTP_TIMEOUT=120 mix:*)",
      "Bash(yes | mix phx.gen.auth Accounts User users --binary-id 2>&1)",
      "Bash(psql -U alexey -d postgres -c \"SELECT 1\" 2>&1 || psql -U postgres -d postgres -c \"SELECT 1\" 2>&1)",
      "Bash(docker rm -f maximum_of_english_pg 2>&1 && docker run -d --name maximum_of_english_pg -e POSTGRES_PASSWORD=postgres -e POSTGRES_USER=postgres -p 5433:5432 postgres:14 2>&1)",
      "Bash(MIX_ENV=test mix ecto.create 2>&1 && MIX_ENV=test mix ecto.migrate 2>&1)",
      "Bash(find:*)"
    ]
  }
}
